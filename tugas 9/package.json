{

  "name": "test",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha test --timeout 10000 --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=mochawesome,html=true,json=true,overwrite=true,quiet=true",
    "report": "mocha test --timeout 10000 --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=mochawesome,html=true,json=true,overwrite=true,quiet=true"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "selenium-webdriver": "^4.46.0"
  },
  "devDependencies": {
    "mocha": "^11.7.6",
    "mocha-simple-html-reporter": "^2.0.0",
    "mochawesome": "^8.0.1"
  }
}
