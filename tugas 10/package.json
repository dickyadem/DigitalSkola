{

  "name": "test",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha specs --timeout 30000 --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=mochawesome,html=true,json=true,overwrite=true,quiet=true",
    "report": "mocha specs --timeout 30000 --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=mochawesome,html=true,json=true,overwrite=true,quiet=true"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "selenium-webdriver": "^4.46.0"
  },
  "devDependencies": {
    "mocha": "^11.7.6",
    "mocha-simple-html-reporter": "^2.0.0",
    "mochawesome": "^8.0.1",
    "pixelmatch": "^7.1.0",
    "pngjs": "^7.0.0"
  }
}
